import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container:{
        margin:10,
        padding:5,
        backgroundColor:'white',
        borderRadius:8,
        flexDirection:"row",
        alignItems:"center"
    },
    input:{
        flex:1
    }
})